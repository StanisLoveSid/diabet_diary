<%= form_for current_user.years.new do |f| %>
<%= f.text_field :created_at %>
<%= f.submit "submit" %>
<% end %>

<% @years.each do |year| %>
<%= link_to "#{year.created_at.year}", year_path(year.id) %>
<% end %>
